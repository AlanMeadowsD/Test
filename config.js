/*
 * Keep real API keys on a server or serverless function, never in this file.
 * Copy config.example.js over this file only if you need to set public endpoint URLs.
 */
window.BEARINGS_CONFIG = window.BEARINGS_CONFIG || {};
